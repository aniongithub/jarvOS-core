get_filename_component(ZP_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${ZP_CMAKE_DIR})

if(NOT TARGET zeropoint::zeropoint)
    include("${ZP_CMAKE_DIR}/zeropointTargets.cmake")
endif()

set(ZP_LIBRARIES zeropoint::zeropoint)