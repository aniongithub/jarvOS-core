get_filename_component(pv_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${pv_CMAKE_DIR})

if(NOT TARGET Polyverse::Polyverse)
    include("${pv_CMAKE_DIR}/PolyverseTargets.cmake")
endif()

set(pv_LIBRARIES Polyverse::Polyverse)